#include <stdio.h>
/**
 * main - printing all single digits number of base 10
 * starting with 0
 * Return: Always 0 (success)
 */
int main(void)
{
	int num;

	for (num = 0; n < 10; ++num)
		printf('%d', num);
		printf("\n");
	return (0);
