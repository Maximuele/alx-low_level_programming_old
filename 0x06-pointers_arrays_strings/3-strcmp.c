#include "main.h"
/**
* _strcmp - a function to compare string
* @s1 - first parameter
* @s2 -second parameter
* return: interger
*/
int _strcmp(char *s1, char *s2)
{
	int result;

	reult = strcmp(s1, s2);
	return (result);
}

